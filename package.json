{
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "common"
  ],
  "scripts": {
    "build": "npm run build -w backend",
    "build:backend": "npm run build -w backend",
    "deploy:backend": "npm run build:backend && npx wrangler deploy backend/src/index.ts --name medium-blogging --compatibility-date 2026-02-12"
  },
  "dependencies": {
    "@medium-blogging/common-app": "^1.0.0",
    "axios": "^1.13.2"
  }
}